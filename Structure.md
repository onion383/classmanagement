# 代码结构

## 前端 `student-frontend`

### 资源文件夹`src`
> 这里是写代码的地方
#### 入口文件 `main.js`
1. 引入必要的东西
- 引入应用实例
- 引入页面布局 `App.vue`
- 引入路由配置 `src/router/index.js`
- 引入`axios`,用于收发`http`请求
- 引入全局样式文件 `style.css`

2. 引入请求拦截器
- 只有浏览器本地有token，后续才能发送请求

3. 创建应用实例
- 创建应用实例 `app.vue`
- 注册路由

#### 主页面布局 `App.vue`
- 登录页面的全屏显示
- 其他页面的左右排布显示

#### 全局样式 `style.css`
- 全局的样式写在这里

### 通用组件函数（复合式函数） `composables文件夹`
#### `useTableCRUD.js`通用表格逻辑
- 获取后端拿出来的表格数据
- 搜索表格数据
- 排序表格数据
- 新增行
- 编辑单元格
- 删除行与批量删除
- 右键菜单
- 剪切粘贴数据
- 导入导出数据（需要配合组件）
- 去除重复数据
- 列管理
>有两个页面的表格处理函数逻辑是一样的，在这里统一提取出来，换上不同的表和api，处理同样逻辑的类型

#### `useSeatGrid.js`座位表的通用逻辑
- 座位表的处理的核心逻辑

### `components`组件文件夹

#### `ConfirmDialog.vue`通用弹窗
- 普通确认弹窗
- 添加列弹窗
- 重命名列弹窗

#### `ContextMenu.vue`右键弹窗

#### `DeduplicateDialog.vue` 处理重复表格设置弹窗
- 检测自动重复数据
- 每一组保留一行
- 选择删除多余的行
- 传递给后端执行删除

#### `DynamicTable.vue` 通用表格组件
- 只负责渲染表格
- 调用`useTableCRUD.js`通用表格逻辑处理

#### `ExportExcel.vue` 导出表格组件
- 将表格数据导出为excel，pdf，csv，HTML网页
- 弹窗选择导出

#### `GridView.vue`通用网状表格组件
1. 网状表格的基本逻辑
- 网格渲染	
- 讲台行	
- 走廊列	
- 时段/节次/时间列	
- 行号列	
- 单元格编辑	
- 候选列表	
- 单元格拖拽交换	
- 行拖拽交换	
- 列拖拽交换	
- 课程颜色	
- 区域选择	

#### `ImportExcel.vue`导入表格组件
- 将表格文件`xlsx`,`csv`,`xls`里面的数据导入软件

#### `Sidebar.vue`侧边栏组件
- 负责显示和导航侧边栏

#### `SearchBar.vue` 搜索框组件

### `widget`小工具文件夹

#### 

## 后端 `student-backend`
